# Runtime dependencies
numpy>=1.26
opencv-python>=4.8

# Hardware-only, required when deploying on the Pi:
# picamera2>=0.3   # Pi HQ Camera capture (camera.py)
# evdev>=1.6       # USB barcode scanner (scanner.py); optional, the app
#                  # falls back to manual typing without it

# Development and CI dependencies are in requirements-dev.txt
