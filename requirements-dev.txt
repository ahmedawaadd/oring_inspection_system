# Development and CI dependencies. The headless OpenCV build avoids
# pulling GUI libraries onto build machines; the app itself needs the
# full opencv-python on the Pi.
numpy>=1.26
opencv-python-headless>=4.8
pytest>=8.0
pytest-cov>=5.0
pyflakes>=3.2
